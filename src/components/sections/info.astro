---
import InfoCard from '../ui/InfoCard.astro'
import Title from '../ui/Title.astro'

import { useTranslations } from '../../i18n/utils'

const { lang } = Astro.params as any
const t = useTranslations(lang)

const testimonialsKeys = ['Marco']
const info

---